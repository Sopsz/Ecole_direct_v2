models:
  Classroom:
    label: string
    letter: string
    user_id: unsigned integer
    relationships:
      belongsTo: User

  Transport:
    label: string
    color: string
    points: unsigned integer
    relationships:
      belongsToMany: User
